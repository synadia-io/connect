{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "io.synadia.connect.v1.spec.connector.steps.source",
  "title": "SourceStepSpec",
  "description": "The external system that is the source of the messages",
  "type": "object",
  "properties": {
    "type": {
      "type": "string",
      "description": "The type of the source step. This should be a source that is included in the connector's runtime"
    },
    "config": {
      "type": "object",
      "description": "The configuration of the source step",
      "additionalProperties": {}
    }
  },
  "required": ["type", "config"]
}