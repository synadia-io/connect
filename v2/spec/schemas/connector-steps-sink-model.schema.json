{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "io.synadia.connect.v1.spec.connector.steps.sink",
  "title": "SinkStepSpec",
  "description": "The external system that is the target for the messages",
  "type": "object",
  "properties": {
    "type": {
      "type": "string",
      "description": "The type of the sink step. This should be a sink that is included in the connector's runtime"
    },
    "config": {
      "type": "object",
      "description": "The configuration of the sink step",
      "additionalProperties": {}
    }
  },
  "required": ["type", "config"]
}